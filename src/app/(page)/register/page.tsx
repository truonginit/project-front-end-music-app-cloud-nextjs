import type { Metadata } from "next";

// Metadata
export const metadata: Metadata = {
  title: "Đăng ký",
  description: "Ứng dụng nghe nhạc trực tuyến",
};

export default function Register() {
    return (
        <>
          <h1>TRANG DANH ĐĂNG KÝ</h1>
        </>
      );
}
